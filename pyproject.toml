[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dirigo"
version = "0.1.0"
description = "Dirigo is a collection of interfaces for customizable image data acquisition"
authors = [
    {name = "T. D. Weber", email = "tweber@mit.edu"}
]
license = {text = "MIT"}
dependencies = []

[project.entry-points."dirigo_acquisitions"]
linescan = "dirigo.builtins.acquisitions:LineAcquisition"
frame = "dirigo.builtins.acquisitions:FrameAcquisition"